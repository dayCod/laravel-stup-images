## What does this PR do?

<!-- Describe the change and link the related issue, e.g. "Fixes #12". -->

## Checklist

- [ ] Targets the `dev` branch
- [ ] Tests added or updated (`composer test`)
- [ ] Larastan passes (`composer analyse`)
- [ ] Code style passes (`vendor/bin/pint --test`)
- [ ] `README.md` updated for public API changes
- [ ] `CHANGELOG.md` updated (and `UPGRADE.md` for breaking changes)
